import React from "react";
import { Text, View } from "react-native";

export default () => (
  <View
    style={{
      justifyContent: "center",
      alignItems: "center",
      flex: 1,
    }}>
    <Text>Welcome</Text>
  </View>
);
